# MB Demo Full Project

Gói gồm:

- `mobile/` — Flutter iOS app demo
- `admin/` — PHP web quản lý
- `admin/data/*.json` — dữ liệu, KHÔNG MySQL và KHÔNG SQLite
- `docs/DEPLOY.md` — hướng dẫn VPS/build IPA

Kiến trúc:

iPhone/iOS App
      |
      v
PHP JSON API
      |
      v
JSON files

Admin Web
      |
      v
JSON files

Đây là hệ thống DEMO. Không kết nối hệ thống ngân hàng thật, không tạo giao dịch ngân hàng thật và không giả mạo ứng dụng ngân hàng chính thức.
